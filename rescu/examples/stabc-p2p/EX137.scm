/* Transcoded from AUT with aut2scm */

scm not_named :

nb_channels = 1 ;
parameters :
real AAA ;

automaton A0 :

initial : 0


state 0 :
to 0 : when true , 0 ! AAA;

automaton A1 :

initial : 0


state 0 :
to 0 : when true , 0  ? AAA;
